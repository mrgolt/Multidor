version: '3'
services:
  multidor:
    build:
      context: .
      dockerfile: Dockerfile
    command: gunicorn -w 4 -b 0.0.0.0:8000 multidor.wsgi:application  # Замените "your_project" на имя вашего Django-проекта
    ports:
      - 8000:8000
  nginx:
    image: nginx:latest
    container_name: nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf
      - ./multidor/templates:/app/multidor/templates
      - ./multidor/media:/app/multidor/media
    depends_on:
      - multidor